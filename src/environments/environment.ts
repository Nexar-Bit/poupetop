export const environment = {
  production: false,
  // Use empty string for development to allow proxy to intercept requests
  // The proxy.conf.json will forward these to https://meumercadohoje.com.br
  apiUrl: ''
};
